# Task Breakdown

- `TASK-001`: Bootstrap `ROS2_DASHBOARD` monorepo with web, control, streaming, shared, and docs packages.
- `TASK-002`: Mirror the Unity `DrivingTrainerUI` landing screen in Next.js.
- `TASK-003`: Add session start flow from landing page to simulation page.
- `TASK-004`: Implement control API endpoints for start, status, and stop.
- `TASK-005`: Bundle the official Render Streaming signaling/web app into the monorepo.
- `TASK-006`: Add the Unity Render Streaming package and standalone build script.
- `TASK-007`: Build and launch the Unity standalone player with signaling URL arguments.
- `TASK-008`: Validate local LAN behavior and document run steps.
